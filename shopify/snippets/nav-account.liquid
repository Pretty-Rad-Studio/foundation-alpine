{% assign nav_link_class = 'text-sm font-bold mr-7 py-2 uppercase hover:underline hidden lg:block' %}

{% if shop.customer_accounts_enabled %}
  <a href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}" class="{{ nav_link_class }}">
    Account
  </a>
{% endif %}

<div class="{{ nav_link_class }} relative flex py-3 group/item mr-7">
  <span> EN (US) </span>

  <ul class="absolute hidden rounded-md group/edit top-2 lg:group-hover/item:flex">
    <li class="p-3">
      <button>
        EN (US)
      </button>
    </li>

    <li class="p-3">
      <button>
        EN (US)
      </button>
    </li>
  </ul>
</div>

<div class="{{ nav_link_class }}">
  USD $$$
</div>

<button class="hidden mr-6 hover:text-opacity-85 lg:block">
  {% render 'icon-search' %}
</button>

<a href="#" class="py-1 pill pill-white">
  Cart
</a>